% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_top_unmapped_source_values.R
\name{export_top_unmapped_source_values}
\alias{export_top_unmapped_source_values}
\title{Export top unmapped source values to CSV}
\usage{
export_top_unmapped_source_values(
  con,
  output_path = "mapping_quality_results/top_unmapped_source_values.csv",
  limit = 500L,
  config = NULL,
  template_for_custom_mapping = FALSE
)
}
\arguments{
\item{con}{DBI connection to the DuckDB database.}

\item{output_path}{Path to the output CSV file. Default: \code{mapping_quality_results/top_unmapped_source_values.csv}.}

\item{limit}{Maximum number of rows per domain. Default 500.}

\item{config}{Optional list with \code{schemas$cdm}; if NULL, uses \code{cdm}.}

\item{template_for_custom_mapping}{If TRUE, adds empty \code{concept_id} column for use as custom mapping CSV.}
}
\value{
Invisible path to the written file.
}
\description{
Queries each CDM clinical table for source values where concept_id = 0,
aggregates by source_value and record count, and writes to a CSV with
\code{source_value}, \code{record_count}, \code{source_table}, and \code{domain}.
Domain is inferred from the CDM table (condition, drug, measurement, procedure, observation).
}
