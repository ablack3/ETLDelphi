% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ndc_utils.R
\name{lookup_ndc_smart}
\alias{lookup_ndc_smart}
\title{Smart NDC lookup: try all normalized variants against OMOPHub}
\usage{
lookup_ndc_smart(raw_ndc, oh_client = NULL, hc_client = NULL)
}
\arguments{
\item{raw_ndc}{Raw NDC string from source data.}

\item{oh_client}{An \code{omophub_client} object.}

\item{hc_client}{A \code{hecate_client} object (for relationship lookup fallback).}
}
\value{
List with \code{ndc_concept} (the NDC concept found), \code{standard_concept}
(the RxNorm standard concept it maps to), \code{matched_variant} (which format matched),
or \code{list(found = FALSE)} if no variant matches.
}
\description{
Generates all plausible NDC variants from a raw string and tries each
against OMOPHub's by-code endpoint. Returns the first successful match
along with the standard (RxNorm) concept via "Maps to" relationship.
}
