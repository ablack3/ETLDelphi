% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/init_vocabulary_db.R
\name{init_vocabulary_db}
\alias{init_vocabulary_db}
\title{Initialize a fresh DuckDB database with DDL and load vocabulary files in batches}
\usage{
init_vocabulary_db(
  db_path,
  vocabulary_dir = NULL,
  cdm_schema = "main",
  delimiter = "\\t"
)
}
\arguments{
\item{db_path}{Character. Path to the DuckDB file (created if missing), or
\code{":memory:"} for an in-memory database.}

\item{vocabulary_dir}{Character. Directory containing vocabulary TSV/CSV files
(e.g. \code{CONCEPT.csv}, \code{VOCABULARY.csv}).}

\item{cdm_schema}{Character. Schema name for CDM/vocabulary tables (default \code{"main"}).}

\item{delimiter}{Character. Column delimiter in vocabulary files (default \code{"\\t"} for TSV).
Use \code{","} if files are comma-separated.}
}
\value{
The DBI connection (invisibly). If \code{con} was supplied, the same connection;
otherwise a new connection that the caller should close with \code{DBI::dbDisconnect(con)}.
}
\description{
Creates a DuckDB database, runs the OMOP CDM DDL (including
vocabulary tables), then loads vocabulary TSV/CSV files from a directory
in batches with a progress bar. After loading, applies primary key constraints,
foreign key constraints, and indexes on vocabulary tables only (not on other
CDM tables). Handles large files (e.g. CONCEPT) by chunked reading and appending.
}
