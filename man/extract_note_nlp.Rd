% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_note_nlp.R
\name{extract_note_nlp}
\alias{extract_note_nlp}
\title{Extract structured data from SOAP notes and populate note_nlp}
\usage{
extract_note_nlp(
  con,
  config = NULL,
  log_path = "note_nlp_mapping_log.csv",
  provider = NULL,
  model = NULL,
  api_key = NULL,
  hecate = NULL,
  skip_llm = FALSE,
  skip_load = FALSE
)
}
\arguments{
\item{con}{DBI connection to the DuckDB database (post-ETL).}

\item{config}{ETL config list. Default: \code{default_etl_config()}.}

\item{log_path}{Path for the concept mapping log CSV.}

\item{provider}{LLM provider for unmatched entities: \code{"anthropic"} or
\code{"openai"}. Default: auto-detected.}

\item{model}{LLM model name. Default: from env var.}

\item{api_key}{LLM API key. Default: from env var.}

\item{hecate}{Hecate client object. Default: created from env vars.}

\item{skip_llm}{If TRUE, only use vocabulary lookup (no LLM calls).}

\item{skip_load}{If TRUE, only do concept mapping (don't load CDM tables).}
}
\value{
Invisible data.frame of mapping results.
}
\description{
Parses SOAP notes into sections, extracts clinical entities (conditions,
prescriptions, procedures, screenings) using regex, maps them to OMOP
concepts via vocabulary lookup and LLM, then loads into \code{cdm.note_nlp}
and supplements \code{cdm.condition_occurrence}, \code{cdm.drug_exposure},
and \code{cdm.procedure_occurrence}.
}
\section{Pipeline integration}{

\preformatted{
# 1. Run ETL (includes note parsing + entity extraction in 35_nlp step)
run_etl(con)

# 2. Map entities and load note_nlp + supplemental facts
extract_note_nlp(con)
}
}

