% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_unmapped_drugs.R
\name{export_unmapped_drugs}
\alias{export_unmapped_drugs}
\title{Export unmapped drugs (name, NDC, record counts) for custom mapping}
\usage{
export_unmapped_drugs(con, output_path = "unmapped_drugs.csv", config = NULL)
}
\arguments{
\item{con}{DBI connection to the DuckDB database.}

\item{output_path}{Path to the output CSV file. Default: \code{unmapped_drugs.csv} in current directory.}

\item{config}{Optional list with \code{schemas$stg}; if NULL, uses \code{stg}.}
}
\value{
Invisible path to the written file.
}
\description{
Writes drugs that did not map to an OMOP concept (drug_concept_id = 0 in
\code{stg.map_drug_order}) to a CSV with \code{drug_name}, \code{drug_ndc_normalized},
\code{drug_ndc_raw} (sample), \code{n_occurrences}, and \code{target_concept_id}
(empty for you to fill). Use for custom mapping (e.g. ingredient-level) when
NDC is missing, wildcard (e.g. 59630*70248), or ambiguous. Run after ETL.
}
