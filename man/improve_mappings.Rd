% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/improve_mappings.R
\name{improve_mappings}
\alias{improve_mappings}
\title{Improve unmapped concept mappings using an LLM with vocabulary search tools}
\usage{
improve_mappings(
  con,
  config = NULL,
  domains = c("condition", "drug", "measurement", "procedure", "observation"),
  limit = 50L,
  confidence_threshold = 0.7,
  custom_mapping_path = NULL,
  custom_ndc_mapping_path = NULL,
  log_path = "mapping_improvement_log.csv",
  provider = NULL,
  model = NULL,
  api_key = NULL,
  hecate = NULL,
  omophub = NULL,
  force_retry = FALSE,
  dry_run = FALSE
)
}
\arguments{
\item{con}{DBI connection to the DuckDB database (post-ETL, staging + CDM tables must exist).}

\item{config}{ETL config list (for schema names). Default: \code{default_etl_config()}.}

\item{domains}{Character vector of domains to improve. Default: all five.}

\item{limit}{Max unmapped source values to process per domain (ordered by record count).}

\item{confidence_threshold}{Minimum confidence to write to production CSV. Default: 0.7.}

\item{custom_mapping_path}{Path to production custom_concept_mapping.csv.
Default: from config or package default.}

\item{custom_ndc_mapping_path}{Path to custom NDC mapping CSV. Default: from config or package default.}

\item{log_path}{Path to detailed log CSV. Default: \code{"mapping_improvement_log.csv"}.}

\item{provider}{LLM provider: \code{"anthropic"} or \code{"openai"}. Default:
auto-detected from API keys (prefers Anthropic if both are set).}

\item{model}{Model name. Default: \code{ANTHROPIC_MODEL} / \code{"claude-sonnet-4-20250514"}
for Anthropic, \code{OPENAI_MODEL} / \code{"gpt-4o"} for OpenAI.}

\item{api_key}{API key. Default: from \code{ANTHROPIC_API_KEY} or \code{OPENAI_API_KEY}
env var based on provider.}

\item{hecate}{Hecate client object. Default: created from env vars.}

\item{omophub}{OMOPHub client object. Default: created from env vars. Used for NDC lookups.}

\item{force_retry}{Controls retry behaviour for already-processed values.
\itemize{
\item \code{FALSE} (default): skip all values already in the log.
\item \code{"failed"}: retry values where the log has concept_id = 0/NA or confidence below threshold.
\item \code{TRUE}: ignore the log entirely — reprocess everything.
}}

\item{dry_run}{If TRUE, show what would be processed without calling LLM.}
}
\value{
Invisible data.frame of all log results.
}
\description{
Queries DuckDB staging tables for records that failed vocabulary mapping
(concept_id = 0), then for each unmapped source value, uses an LLM (Claude
or GPT-4) with Hecate semantic search and OMOPHub code lookup tools
to find the best OMOP concept mapping.
}
\details{
The function provides the LLM with rich context: both the source code AND
the human-readable name for each unmapped value (e.g., both the NDC code
and the drug name), enabling more accurate mapping.

Results are written to a detailed log CSV and, for high-confidence mappings,
appended to the custom_concept_mapping.csv and custom_ndc_mapping.csv files
that the ETL consumes.
}
\section{Pipeline integration}{

\preformatted{
# 1. Run ETL
run_etl(con)

# 2. Improve unmapped concepts
improve_mappings(con, limit = 50, confidence_threshold = 0.7)

# 3. Re-run ETL from vocab step to pick up new mappings
run_etl(con, from_step = "30_vocab")
}
}

